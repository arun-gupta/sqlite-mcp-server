{
  "name": "sqlite-mcp-server",
  "version": "1.0.0",
  "description": "Model Context Protocol server for SQLite database operations",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
            "start": "node src/server.js",
        "dev": "node --watch src/server.js",
        "test": "node test/test.js",
        "http": "node src/http-wrapper.js",
        "docker:build": "docker build -t sqlite-mcp-server .",
        "docker:run": "docker run -d --name sqlite-mcp -v $(pwd)/data:/data -e SQLITE_DB_PATH=/data/database.db sqlite-mcp-server",
        "docker:run-http": "docker run -d --name sqlite-mcp-http -p 4000:4000 -v $(pwd)/data:/data -e SQLITE_DB_PATH=/data/database.db -e HTTP_PORT=4000 -e SERVER_MODE=http sqlite-mcp-server",
        "docker:stop": "docker stop sqlite-mcp sqlite-mcp-http 2>/dev/null || true",
        "docker:clean": "docker rm sqlite-mcp sqlite-mcp-http 2>/dev/null || true"
  },
  "keywords": [
    "mcp",
    "sqlite",
    "database",
    "model-context-protocol"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "sqlite3": "^5.1.7",
    "@modelcontextprotocol/sdk": "^0.4.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
